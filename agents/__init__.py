"""Agent layer for meridian-capital.

Currently stubs — the chat view in `views/chat.py` consumes seeded fixtures
from `data.py` (CHAT_SEED, FOLLOWUP_RESPONSE). These modules define the
contract that real agents will satisfy when the seeded chat is swapped for
live orchestration. See docs/AGENTS.md for the full schema.
"""
